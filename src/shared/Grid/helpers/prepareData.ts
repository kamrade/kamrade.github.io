import { RuleVeto } from '../rule-veto.types';
import { ISortedBy } from '../grid.types';

// TODO: implementation
// Here is the place where data should be - sorted, filtered, paginated, calculate statistic
export const prepareData = (data: RuleVeto[], sortedBy: ISortedBy) => data;